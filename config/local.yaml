localization_node:
ros__parameters:

```
use_sim_time: true

map_topic: /map_lidar
scan_topic: /scan
odom_topic: /odom

pose_topic: /estimated_pose

map_frame: map
odom_frame: odom
base_frame: base_link
```
